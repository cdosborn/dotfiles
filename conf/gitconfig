[user]
	name = Connor Osborn
	email = cdosborn@email.arizona.edu
[core]
	editor = vim
[push]
	default = matching
[alias]
	co = checkout
        ca = diff --cached 
        bname = rev-parse --abbrev-ref HEAD
        re = remote -v
	edit = !vim -o `git diff --diff-filter=AM --name-only  --cached`
[mergetool]
	tool = vimdiff
[color]
	ui = auto
