[user]
	name = Connor Osborn
	email = cdosborn@email.arizona.edu
[core]
	editor = vim
[push]
	default = simple
[alias]
	co = checkout
        br = branch --sort=committerdate
        ca = diff --cached
        bname = rev-parse --abbrev-ref HEAD
        re = remote -v
	edit = !$EDITOR -o `git ls-files --modified | sort -u`
        fixup = commit --amend -C @
        ls = ls-files
        diffls = diff --name-only
        index = ls-files --stage
[mergetool]
	tool = vimdiff
[color]
	ui = auto
