[user]
	name = Connor Osborn
	email = cdosborn@email.arizona.edu
[core]
	editor = vim
[push]
	default = matching
[alias]
	co = checkout
        ca = diff --cached
        bname = rev-parse --abbrev-ref HEAD
        re = remote -v
	edit = !$EDITOR -o `git ls-files --modified`
        ls = ls-files
        diffls = diff --name-only
[mergetool]
	tool = vimdiff
[color]
	ui = auto
