# Emulate 256 color mode
term    screen-256color

startup_message off

# Enable the alternate window
altscreen on

# the - means login
defshell bash

# lines of scrollback!
defscrollback 4096

backtick 1 5 5 gawk -v width=15 -v sleep=5 -f /Users/cdosborn/proj/awk/progress.awk
hardstatus alwayslastline
hardstatus string ' %H  %1` '
caption string ' %{= kw}%-w%?%{-}%?%F%{= dk} %n %t %?%?%{= kw}%+w %?%{= kk}'
